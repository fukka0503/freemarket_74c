.management-main
  .main-box
    .main-box-item
      .main-title
        会員情報入力
  .main-form
    .main-form-top
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = devise_error_messages!
        .field
          .field-label
            = f.label :ニックネーム
          .field-required
            = f.label :必須
        .field-input
          = f.text_field :nickname, class: 'field-input-text', autofocus: true, placeholder: '例)フリマ太郎'
        .field
          .field-label
            = f.label :email
          .field-required
            = f.label :必須
        .field-input
          = f.email_field :email, class: 'field-input-text', placeholder: 'PC・携帯どちらでも可'
        .field
          .field-label
            = f.label :パスワード
          .field-required
            = f.label :必須
        .field-input
          = f.password_field :password, class: 'field-input-text', autocomplete: "off", placeholder: '8文字以上の半角英数字'
        .field
          .field-label
            = f.label :パスワード（確認）
          .field-required
            = f.label :必須
        .field-input
          = f.password_field :password_confirmation, class: 'field-input-text', autocomplete: "off", placeholder: '8文字以上の半角英数字'
        .form-info-text
          %p.note ※英字と数字両方含めて設定してください
          %h3.Identification 本人確認
          %p.Heads-up 安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
        .field
          .field-label
            = f.label :お名前（全角）
          .field-required
            = f.label :必須
        .field-input-name
          = f.text_field :family_name, class: 'field-input-family', autofocus: true, placeholder: '例)山田'
          = f.text_field :first_name, class: 'field-input-first', autofocus: true, placeholder: '例)太郎'
        .field
          .field-label
            = f.label :お名前カナ（全角）
          .field-required
            = f.label :必須
        .field-input-name
          = f.text_field :family_furigana, class: 'field-input-family', autofocus: true, placeholder: '例)ヤマダ'
          = f.text_field :first_furigana, class: 'field-input-first', autofocus: true, placeholder: '例)タロウ'
        .explain
          .field-btd
            = f.label :生年月日
          .field-absolutely
            = f.label :必須
          .field-write
            = f.label :例1995年6月25日→19950625
        .field-input
          = f.text_field :birthday, class: 'field-input-text', autocomplete: true, placeholder: '例)19950625'
        .actions
          = f.submit "次へ", class: "btn"